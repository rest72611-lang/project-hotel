# Postman Export

This folder contains the Postman material for the backend submission.

Main environment file:

- `project-hotel.postman_environment.json`

Additional environment files:

- `project-hotel.docker.postman_environment.json`
- `project-hotel.localhost.postman_environment.json`

Request definitions are available in:

- `collections/`
